import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  margin-bottom: 2rem;
`;

export const Span = styled.span`
  position: absolute;
  display: block;
  bottom: -1rem;
  right: 0;
  font-size: 0.8rem;
  color: #dd3523;
`;
